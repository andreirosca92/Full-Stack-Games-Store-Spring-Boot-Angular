import { ClickDirectiveDirective } from './click-directive.directive';

describe('ClickDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
